# Personal Access Token com acesso a dados públicos (public_repo/read).
# Local: pode gerar com `gh auth token`.
GITHUB_TOKEN=

# URL pública do site (produção) — resolve as OG images para URL absoluta (necessário para o unfurl no LinkedIn).
NEXT_PUBLIC_SITE_URL=https://seu-dominio.vercel.app
